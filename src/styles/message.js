import { primaryColor, mainColor, mainColorPressed, subTitleColor } from '../common/constants'

export default {
  scrollView: {
    backgroundColor: mainColorPressed,
  },
  endText: {
    alignSelf: 'center', 
    color: subTitleColor, 
    fontWeight: 'bold', 
    opacity: 0.8
  },
}